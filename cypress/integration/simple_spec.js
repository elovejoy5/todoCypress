describe('Hello World Test', 
  function() {
    it(
      'A test that should pass', function() {
      expect(true).to.equal(true)
      }
    )

    // it(
    //   'A test that should fail', 
    //   function() {
    //   expect(true).to.equal(false)
    //   }
    // )
})